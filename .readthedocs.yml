version: 2

submodules:
  exclude: all

sphinx:
  configuration: documentation/conf.py

python:
  install:
    - requirements: requirements/documentation.txt

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"
