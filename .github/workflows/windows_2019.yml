name: Windows 2019

on:
  pull_request:
    branches: [ main ]

jobs:
  collect_lib_lib:
    name: Collect lib/lib*
    runs-on: windows-2019
    defaults:
      run:
        shell: bash -l {0} # Source profile for each step
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive
      - run: python3 build-system/install.py
      - run: erbb setup
      - uses: actions/upload-artifact@v3
        with:
          name: msys64_mingw64_lib_lib
          path: C:/msys64/mingw64/lib/lib*
