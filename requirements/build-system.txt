# Packages required to user erbb and erbui

# SVG & PDF generation

cairocffi>=1.4.0,<1.5.0
CairoSVG>=2.6.0
freetype-py>=2.4.0

# DXF generation

ezdxf>=1.0.2

# Kicad module generation from SVG

svg2mod>=1.2

# AudioSample generation

# SoundFile 0.14.0 evaluates numpy.ndarray [tuple [int, ...], numpy.dtype [...]]
# at import time, which requires numpy 1.22 or later. Our Ubuntu 22.04 workflow
# ships with numpy 1.21.5, so keep Soundfile below 0.14.
SoundFile>=0.12.1,<0.14
numpy>=1.21.0

# Display Format

imageio>=2.31.1

# ST-link detection

pyserial==3.5
